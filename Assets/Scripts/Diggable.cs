using UnityEngine;

[CreateAssetMenu(fileName = "Diggable", menuName = "Custom/Diggable Object", order = 1   )]
public class Diggable : ScriptableObject
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
